package gerrit.retief.upskilling.solid.greeter;

/**
 * ISP (Interface segregation principle): Separate interfaces for different behaviors instead of one big interface.
 */
interface Greeter {
    void greet();
}